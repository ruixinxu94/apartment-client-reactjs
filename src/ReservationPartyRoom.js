import React from 'react';

function ReservationPartyRoom(props) {
    let {reservationPartyRoom} = props;
    return (
        <span>{reservationPartyRoom}</span>
    );
}
export default ReservationPartyRoom;
